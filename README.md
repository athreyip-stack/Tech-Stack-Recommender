# Tech-Stack-Recommender
The Tech Stack Recommender is a machine learning application that recommends suitable career paths based on a user's technical skills. It uses **TF-IDF** to convert skill sets into vectors and **Cosine Similarity** to compare them with predefined job profiles, then displays the top three career recommendations with the highest match scores.
